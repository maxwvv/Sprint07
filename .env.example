# URL base da API. Copie este arquivo para `.env` e ajuste se necessário.
# Sem esta variável a aplicação usa a API de mock padrão (ver src/services/api.js).
VITE_API_BASE_URL=https://6891f53f447ff4f11fbe838f.mockapi.io/api/revisao
